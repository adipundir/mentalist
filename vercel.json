{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd .. && pnpm install --frozen-lockfile --filter frontend...",
  "crons": [
    {
      "path": "/api/keeper",
      "schedule": "0 4 * * *"
    }
  ]
}
